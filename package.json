{
  "name": "procted-code-editor-root",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["coding-editor-widget"],
  "scripts": {
    "dev": "npm --prefix coding-editor-widget run dev",
    "build": "npm --prefix coding-editor-widget run build",
    "preview": "npm --prefix coding-editor-widget run preview"
  }
}